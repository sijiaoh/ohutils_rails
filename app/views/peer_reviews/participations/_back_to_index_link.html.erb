<% if policy([:peer_reviews, :participation]).index? %>
  <%= link_to(
    t("back_to_index"),
    [participation.peer_review, :peer_reviews_participations],
    class: "button-secondary"
  ) %>
<% end %>
