<% if policy(participation).destroy? %>
  <%= button_to(
    t("destroy"),
    participation,
    method: :delete,
    class: "button-danger",
    form: { data: { turbo_confirm: t("destroy_confirm") } }
  ) %>
<% end %>
