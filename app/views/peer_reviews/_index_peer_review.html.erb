<div id="<%= dom_id peer_review %>">
  <div class="my-default font-bold"><%= peer_review.title %></div>

  <div class="scaffold-buttons my-default">
    <%= render "show_link", peer_review: %>
    <%= render "edit_link", peer_review: %>
  </div>
</div>
