<% if policy(peer_review).destroy? %>
  <%= button_to(
    t("destroy"),
    peer_review,
    method: :delete,
    class: "button-danger",
    form: { data: { turbo_confirm: t("destroy_confirm") } }
  ) %>
<% end %>
