<% if policy(review).destroy? %>
  <%= button_to(
    t("destroy"),
    review,
    method: :delete,
    class: "button-danger",
    form: { data: { turbo_confirm: t("destroy_confirm") } }
  ) %>
<% end %>
