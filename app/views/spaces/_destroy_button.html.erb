<% if policy(space).destroy? %>
  <%= button_to(
    t("destroy"),
    space,
    method: :delete,
    class: "button-danger",
    form: { data: { turbo_confirm: t("destroy_confirm") } }
  ) %>
<% end %>
