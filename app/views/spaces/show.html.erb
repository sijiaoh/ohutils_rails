<div>
  <%= render @space do %>
    <div class="flex justify-between items-center my-default">
      <h2 class="font-bold text-2xl"><%= t "posts.index.title" %></h2>
      <%= render "posts/new_link", space: @space %>
    </div>

    <turbo-frame
      id="posts"
      src="<%= space_posts_path @space, page: params[:page] %>"
      target="_top"
    ></turbo-frame>
  <% end %>
</div>
