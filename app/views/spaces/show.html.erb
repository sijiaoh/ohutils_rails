<div>
  <div class="scaffold-buttons my-default">
    <%= link_to t("edit"), edit_space_path(@space), class: "button-primary" %>
    <%= button_to t("destroy"), space_path(@space), method: :delete, class: "button-danger" %>
    <%= link_to t("back_to_index"), spaces_path, class: "button-secondary" %>
  </div>

  <%= render @space %>
</div>
